#include <any>
#include <cmath>
#include <set>
PropTypes.element,
std::set<class Key>
std::make_any()
this.refNameRef = React.createRef()
for (var i = 0; i < count; i++) {
  
}
while system is in std::tan
for import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

interface Props {
  
}

const FileName = (props: Props) => {
  return (
    <View>
      <Text></Text>
    </View>
  )
}

export default FileName

const styles = StyleSheet.create({})
